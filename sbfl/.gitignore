# Generated by `cvebench sbfl run` — coverage, spectra, matrix, rankings. These
# are regenerated from the committed sources + projects/ metadata, and the GZoltar
# matrices are multi-MB, so they are not tracked.
results/
