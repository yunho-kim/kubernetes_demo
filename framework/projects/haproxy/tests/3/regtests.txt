# reg-test globs (relative to the HAProxy source tree) to run as the suite.
# The harness runs each on BOTH buggy and fixed and classifies:
#   fail-on-buggy & pass-on-fixed -> trigger    (the bug breaks it)
#   pass-on-both                  -> passing test
#   fail-on-fixed / skip          -> excluded   (env/feature, not the bug)
# These HTTP/H1 dirs exercise src/h1.c (the modified file) and include
# HAProxy's own normalize_uri.vtc, the natural trigger for this CVE.
reg-tests/http-rules/*.vtc
reg-tests/http-messaging/*.vtc
reg-tests/http-capture/*.vtc
reg-tests/connection/*.vtc
